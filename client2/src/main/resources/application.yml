
server:
  port: 8082

spring:
  application:
    name: client2

eureka:
  client:
    service-url:
      default-zone: "http://localhost:8761/eureka/"
    register-with-eureka: true
    fetch-registry: true